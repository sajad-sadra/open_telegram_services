[Unit]
Description=open_telegram_services
Requires=network
After=network

[Service]
Type=simple
Restart=always
RestartSec=1
ExecStart=/usr/bin/bash /usr/local/bin/set_teleg_rules

[Install]
WantedBy=multi-user.target
